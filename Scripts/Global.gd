extends Node

var main: Main
var pats_manager = PatsManager.new()
var cursor_manager = CursorManager.new()

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass
